# 笔皮肤新增流程与设计原则

> 本文是“你画我猜”项目新增一支笔皮肤的完整操作规范。内容以当前项目的实际代码结构为准，目标是让一支笔从主题设计、素材制作，一直走到房间同步、抽卡、特效、测试、发布和回滚都可追踪、可验证。

## 1. 先明确：一支完整的笔皮肤包含什么

笔皮肤不是“加一张 PNG”这么简单。一个可交付的皮肤至少包含下面这条闭环：

```text
稳定 skinId
  -> 素材文件与来源记录
  -> 前端 PEN_SKINS 注册
  -> 笔尖锚点校准
  -> 可选的本地笔迹/落笔特效
  -> 服务端白名单与房间同步
  -> 稀有度与抽卡池
  -> 传说播报（仅传说皮肤）
  -> 本机、远端、移动端、低动效验证
  -> 清单、版本、回滚记录
```

缺少其中任何一环，都可能出现“自己能看到、别人看到默认笔”“笔尖和实际落点分离”“抽到了但没有特效”“刷新后皮肤消失”等问题。

### 当前代码中的真实入口

| 责任 | 文件/符号 | 说明 |
| --- | --- | --- |
| 前端皮肤注册 | `public/app.js` 的 `PEN_SKINS` | 运行时显示名、稀有度、资源文件、`effectId` 的主注册表 |
| 稀有度规则 | `public/app.js` 的 `PEN_RARITY_META` | 稀有度名称、重复转化碎片数量 |
| 本地收藏 | `public/app.js` 的 `PEN_STORAGE_KEY`、`defaultPenCollection`、`loadPenCollection` | 收藏、装备、星尘、碎片、抽数、保底数据保存在浏览器本地 |
| 笔尖位置 | `public/app.js` 的 `PEN_CURSOR_ANCHORS`、`positionPenCursor` | 归一化到 64px 光标盒，不直接使用原图坐标 |
| 素材路径 | `public/assets/pen-skins/` | 当前运行时通过 `skin.asset` 组成 `/assets/pen-skins/...` |
| 资源说明 | `public/assets/pen-skins/manifest.json` | 记录皮肤清单、锚点原则、同步原则、来源与授权状态；不是前端运行时唯一注册表 |
| 本地特效 | `public/app.js` 的 `is*PenSkin`、`spawn*`、`spawnPenStartEffect`、`spawnPenEndEffect` | 特效跟随已有落笔/收笔事件在本地渲染 |
| 房间皮肤白名单 | `server.js` 的 `PEN_SKIN_IDS` | 服务端只接受白名单中的 ID |
| 传说白名单 | `server.js` 的 `PEN_LEGENDARY_SKIN_IDS` | 传说抽中播报的安全校验 |
| 装备同步 | `server.js` 的 `set_pen_skin` 分支 | 只同步已批准的 `skinId`，不传图片和特效代码 |
| 传说播报 | `pen_legendary_announce` | 当前房间广播给所有人，包含抽中的本人 |
| 本机测试发抽数 | `pen_gacha_grant` | 仅回环地址用户、游戏进行中可用，服务端再次校验 |
| 页面挂载点 | `public/index.html` 的 `penCursor`、`remotePenCursor`、`penEffectLayer` | 本地笔、远端笔、特效层的 DOM 容器 |

## 2. 不可违反的设计原则

### 2.1 皮肤只改变表现，不改变玩法

皮肤不能影响绘画坐标、笔迹宽度、得分、猜词判定、倒计时、网络事件顺序或胜负结果。`skinId` 是装饰数据，不是玩法参数。

### 2.2 ID 一旦发布就不可复用

使用小写、短横线分隔的稳定 ID，例如：

```text
dragon-ink
eva-unit01
paimon-starlight
```

不要使用中文、空格、随机数字或会随文案变化的 ID。已经进入用户 `localStorage`、房间状态或历史记录的 ID，不能拿去指向另一支完全不同的笔；下架时保留旧 ID 的经典笔回退路径。

### 2.3 笔尖是动作锚点，不是装饰中心

玩家真正关心的是“我点在哪里，笔尖就落在哪里”。素材再华丽，也不能让笔尖漂在光标外面。素材的头部、挂件、角色、尾巴都要服从锚点，而不是反过来改变绘画落点。

### 2.4 连续笔迹拖尾只走本地配方

当前资源清单仍保持 `positionSync: false`、`skinIdSync: true`；连续尾迹由前端按皮肤配方本地生成：

- 画线数据仍由原有绘画同步逻辑负责；
- 远端只接收轻量的 `skinId` 和已有绘画事件；
- 不发送每一帧的皮肤图片、粒子、CSS 或特效参数；
- 落笔和收笔节点继续使用一次短促的本地爆发；
- 移动画笔时由 `PEN_TRAIL_PROFILES` 选择星尘、音波、剑气、火舌、冰晶、像素块等轻量尾迹，不发送粒子数组；
- 每个皮肤有独立的发射间隔、形状和运动距离，`penEffectLayer` 设置总元素上限并在 CSS 动画结束后自动清理；
- `prefers-reduced-motion: reduce` 会缩短尾迹生命周期，避免持续运动造成负担。

这样既能让别人看到当前画笔，又不会因为皮肤增加一条高频网络通道。

### 2.5 同一事件在本地和远端使用同一套表现规则

特效要从玩家的当前皮肤数据推导，不能由客户端发送一段“请大家执行这段特效代码”。推荐路径是：

```text
远端收到已有 draw_event
  -> 根据 playerId 找到该玩家的 penSkinId
  -> PEN_SKINS 查到 effectId
  -> spawnPenStartEffect / spawnPenEndEffect
  -> 本地创建短生命周期 DOM 粒子
```

如果传说播报需要声音，发送者和其他人也应收到同一个播报事件；发送者是否播放声音可以在本地按 `msg.playerId !== state.playerId` 做去重，但不能把发送者从视觉播报中排除。

### 2.6 稀有度对应表现复杂度，不对应强度

建议关系如下：

| 稀有度 | 设计目标 | 抽中后的表现 |
| --- | --- | --- |
| 免费 | 基础识别、永远可用 | 无特殊特效 |
| 普通 | 清楚、轻量、易读 | 低成本造型 |
| 稀有 | 有明确主题元素 | 少量颜色/形状变化 |
| 史诗 | 造型与动效都更完整 | 落笔/收笔短特效 |
| 传说 | 主题角色或强识别符号 | 专属特效、传说级房间弹幕 |

所有稀有度都必须是纯装饰。抽卡、重复转化、保底和测试发放不能绕过服务端安全校验。

### 2.7 素材必须有来源和授权边界

新增前必须记录：

- 是原创素材、用户提供素材、官方授权素材，还是仅作为风格参考；
- 参考图来自哪里，是否允许用于当前测试环境；
- 是否含有角色、商标、Logo、字体、音乐或第三方音效；
- 若以后公开部署或商业分发，是否需要重新绘制、替换音频或取得授权。

私测可用不等于可以公开分发。尤其是角色联动、品牌主题、游戏/动画形象和 CSGO 风格音效，必须把“参考”与“可直接复制的素材”区分开。

### 2.8 适配手机、平板和低动效设置

- 皮肤预览不能依赖固定的大屏尺寸；
- 抽卡和传说播报必须避开安全区、输入法和系统刘海；
- 特效不能遮挡猜词输入、画板、快捷输入栏和主要操作按钮；
- `prefers-reduced-motion: reduce` 下减少或关闭大范围运动；
- 信息不能只通过颜色表达，稀有度要同时有文字或图标。

### 2.9 对旧数据友好

旧用户可能已经保存了 `selected`、`owned`、碎片、抽数和保底字段。新增皮肤应采用增量方式，不要重置收藏，不要改写旧 ID，不要把未知皮肤直接从用户数据中静默删除。未知或损坏的选中值应回退到 `classic-pencil`，但尽量保留可迁移数据。

## 3. 从需求到上线的完整流程

### 第 0 步：写皮肤需求卡

在动手画素材前，先写清楚：

```text
主题：
显示名：
稳定 ID：
稀有度：free / common / rare / epic / legendary
主体元素：
笔杆与笔尖形态：
落笔特效：有 / 无
收笔特效：有 / 无
连续拖尾：默认无；如有，说明原因与上限
传说房间播报：是 / 否
素材来源与授权：
目标设备：手机 / 平板 / PC
验收截图或录屏：
```

需求卡的作用是防止“只做出一张好看的图，却没有可同步、可回滚的游戏对象”。

### 第 1 步：确定稳定 ID 和元数据

推荐数据形状：

```js
{
  id: 'new-skin-id',
  name: '显示名称',
  rarity: 'epic',
  asset: 'new-skin-id.png',
  effectId: 'new-skin-effect',
  description: '一句话说明主题与表现。'
}
```

规则：

1. `id` 只出现一次，并且一经进入测试环境就不再修改。
2. `asset` 只写文件名，不接受用户输入的任意 URL 或路径。
3. 没有特效时省略 `effectId`，不要填写一个没有实现的假 ID。
4. `description` 简短、可验证，不写“超强”“增加命中率”等玩法暗示。
5. `rarity` 必须是 `PEN_RARITY_META` 已支持的值。

### 第 2 步：制作和整理素材

推荐标准：

- 使用透明背景 PNG；
- 画布尽量为正方形，保证在 64px 光标盒中仍能看清轮廓；
- 笔尖或实际动作端清晰，不要被白边、阴影或挂件淹没；
- 不在图片里烘焙按钮、稀有度文字、弹幕或 UI 边框；
- 不把连续轨迹画进 PNG；
- 保留一份原始素材，运行时使用处理后的副本；
- 不随意覆盖已有同名素材，尤其是已经调过锚点的皮肤。

如果原图带有连续浅色背景，可使用项目脚本做连通背景清理：

```powershell
node scripts/prepare-pen-skin.mjs 输入.png public/assets/pen-skins/new-skin-id.png
```

该脚本只适合清理从边缘连通的浅色背景。它不是万能抠图工具；如果主体本身接近浅色背景，必须人工检查透明边缘、发丝、细线和半透明阴影。

### 第 3 步：做素材 QA

至少检查：

- 文件能否被浏览器直接访问；
- PNG 是否损坏，尺寸是否合理；
- 四周是否有意外白底、黑边、裁切或大块空白；
- 用 `object-fit: contain` 放进预览盒后，主体是否太小；
- 图片实际笔尖和预期锚点是否明确；
- 不同 DPR、手机窄屏、平板横竖屏下是否仍可辨认；
- 是否误带入第三方水印、UI 文本或未授权 Logo。

完成后把文件名、尺寸、处理方式和来源补到 `public/assets/pen-skins/manifest.json`。manifest 是资源审计和交接依据，不能代替前端注册。

### 第 4 步：注册前端皮肤

在 `public/app.js` 的 `PEN_SKINS` 中加入一条数据。示例：

```js
{
  id: 'new-skin-id',
  name: '新皮肤',
  rarity: 'epic',
  asset: 'new-skin-id.png',
  effectId: 'new-skin-effect',
  description: '主题元素与视觉特点。'
}
```

注册后，收藏界面、当前装备、预览、抽卡结果和画手标签才能找到这支笔。不要只把 ID 填进服务端白名单，也不要只把图片放进 assets 目录。

### 第 5 步：校准笔尖锚点

当前锚点是相对于 64px 光标盒的归一化坐标：

```js
const PEN_CURSOR_ANCHORS = Object.freeze({
  'new-skin-id': [x, y]
});
```

`x`、`y` 的范围通常是 `0` 到 `1`：

- `x = 0` 表示光标盒左边，`x = 1` 表示右边；
- `y = 0` 表示顶部，`y = 1` 表示底部；
- `[0.08, 0.92]` 大致适合左下方笔尖；
- 不能套用“图片视觉中心”，要以实际笔尖为准。

校准步骤：

1. 先把素材放到 64px 光标盒中。
2. 在画板上对准明显的十字、网格交叉点或小目标点击。
3. 观察图片尖端与真实绘画起点的水平、垂直偏差。
4. 图片尖端偏右，增大 `x`；图片尖端偏下，增大 `y`；反向则减小。
5. 分别验证左上、右上、左下、右下和中间位置，避免只在一个点上“碰巧对齐”。
6. 把最终值写入 `PEN_CURSOR_ANCHORS` 和 `manifest.json` 的 `overrides`。

锚点是每支皮肤的独立数据，不能为了修一支笔而修改全局默认锚点。

### 第 6 步：决定是否添加专属笔迹特效

只有主题确实能从短特效中受益时才添加。推荐优先级：

1. 轻量尾迹：优先通过 `PEN_TRAIL_PROFILES` 选择一个与主题匹配的形状，不新增网络字段。
2. 落笔特效：在开始绘画位置做一个短促、低数量爆发。
3. 收笔特效：在抬笔位置做一次主题收束。
4. 额外粒子：只在确有主题必要时增加，并设置最大粒子数与自动清理。

实现方式：

1. 在皮肤元数据中添加唯一 `effectId`。
2. 需要爆发时新增对应的 `isXPenSkin(skin)` 判断，按 `effectId` 判断，不按显示名判断。
3. 需要持续表现时在 `PEN_TRAIL_PROFILES` 增加唯一 `kind`、颜色、间隔和距离，复用 `spawnPenTrailEffect`。
4. 需要爆发时新增 `spawnXxx(point, phase)`，使用 `penEffectLayer` 创建短生命周期元素，并接入 start/end 分支。
5. CSS 动画结束后移除元素，避免越画越卡。
6. 在移动端、低动效和连续快速落笔时检查上限。

当前项目已有派蒙、皮卡丘、初音、重音 Teto、Persona 3、方块、龙、初号机、雪王等 effect route。新增皮肤应复用这个分发入口，不要另起一套每帧监听器。

特效原则：

- 特效是本地表现，不发送图片字节、粒子数组或 CSS 字符串；
- 不改变实际笔迹，不叠加无法擦除的画布内容；
- 不遮住题目、画布操作和输入栏；
- 不把特效关键提示放在颜色之外无法理解的地方；
- `prefers-reduced-motion` 下应自然降级。

### 第 7 步：加入服务端白名单

在 `server.js` 中把新 ID 加入 `PEN_SKIN_IDS`：

```js
const PEN_SKIN_IDS = new Set([
  // ...已有 ID
  'new-skin-id',
]);
```

如果稀有度为 `legendary`，还必须加入 `PEN_LEGENDARY_SKIN_IDS`：

```js
const PEN_LEGENDARY_SKIN_IDS = new Set([
  // ...已有传说 ID
  'new-skin-id',
]);
```

服务端的职责是安全和同步，不是相信客户端发来的资源信息：

- `set_pen_skin` 只接受 `PEN_SKIN_IDS` 中的 ID；
- 广播玩家列表时，未知 ID 回退到 `classic-pencil`；
- 客户端不能上传图片路径、`effectId` 或任意脚本让别人执行；
- 传说播报只接受传说白名单中的 ID；
- 服务端应继续保留限频和房间阶段校验。

### 第 8 步：接通房间内远端显示

完整链路应是：

```text
本机装备皮肤
  -> setPenCursorSkin(..., { notifyRoom: true })
  -> WebSocket: { type: 'set_pen_skin', skinId }
  -> server.js 白名单校验
  -> broadcastPlayers(room)
  -> 远端 state.players[].penSkinId
  -> updateRemotePenCursor()
  -> 同一份 PEN_SKINS 找 asset/effectId
```

验证重点：

- 自己切换后，本机立即更新；
- 其他已连接玩家能看到新笔；
- 新加入房间的玩家通过 `room_state` 或玩家列表也能看到；
- 刷新、重连、切换画手后不会退回默认笔；
- 远端不显示另一个人的特效到自己笔上；
- 远端只使用服务端认可的 ID 拼接固定资源路径。

### 第 9 步：接入抽卡和稀有度

当前抽卡逻辑会从 `PEN_SKINS` 中按稀有度选取，处理新皮肤时应先确认：

- `rarity` 能被 `PEN_RARITY_META` 识别；
- 新皮肤不会进入 `free` 抽卡池；
- 重复皮肤会按照该稀有度转成碎片；
- 保底计数、总抽数和 `owned` 数据不被重置；
- 单抽入口仍按现有的单抽规则消耗资源；
- 本机测试模式只用于开发验证，不能当作正式权限系统。

当前收藏数据使用 `dg_pen_collection_v1`。新增字段时要做兼容读取和默认值；不要直接换 key 逃避迁移问题。

### 第 10 步：接入传说弹幕

只有传说皮肤需要房间级传说播报。标准流程：

```text
本机抽中 legendary
  -> announceLegendaryDraw(skin)
  -> pen_legendary_announce
  -> 服务端校验阶段、skinId、频率
  -> broadcast(room, event)；包含发送者
  -> 所有人渲染非阻塞弹幕
```

弹幕要求：

- 不使用阻塞式弹窗；
- 发送者本人也能看到；
- 同一事件不要在发送者和服务端各渲染一次；
- 有队列、超时、最大显示数量，避免连续抽中时堆满屏幕；
- 使用独立层，不依赖普通弹幕开关才能显示关键传说结果；
- `pointer-events: none`，不能挡住画布和输入栏；
- 使用安全区顶部偏移，适配手机和平板；
- 需要音效时，发送者可单独去重播放，视觉事件不能去重掉。

### 第 11 步：本机测试发放抽数

当前本机测试发放是单独的 `pen_gacha_grant` 事件，不应把“本机无限抽”写成公开客户端权限。规则必须保持：

- 服务端检查 WebSocket 对端是否为 `127.0.0.1`、`::1` 或 IPv4 映射的回环地址；
- 只允许游戏进行中发放；
- 单次数量有上限；
- 有冷却，防止按钮连点刷事件；
- 广播给当前房间在线玩家，发送者也包括在内；
- 各客户端把收到的 `bonusDraws` 保存到自己的本地收藏；
- 测试发放弹幕使用独立的特殊层，不能被普通弹幕设置误关；
- 正式部署前必须移除、关闭或改为明确的管理员鉴权。

新增皮肤不需要修改这条发放链路；只要它正确注册进抽卡池，拿到测试抽数后即可验证。

### 第 12 步：处理缓存和版本

前端资源使用了带版本查询参数的缓存破除方式。修改 `public/app.js` 或 `public/style.css` 后：

1. 提升 `public/index.html` 中对应资源的版本号；
2. 确认新 PNG 的文件名与 `asset` 完全一致；
3. 重启 Node 服务端以加载 `server.js` 白名单改动；
4. 浏览器执行硬刷新，并观察 Network/Console；
5. 不要把“本地旧缓存仍显示默认笔”误判成代码未注册。

## 4. 验证清单

### 4.1 静态检查

```powershell
node --check public/app.js
node --check server.js
```

如果只改了皮肤，不需要为了它运行题库审计；`npm run audit:words` 只用于题库变更。

### 4.2 启动与 HTTP 检查

```powershell
npm start
```

另开 PowerShell 检查页面和素材：

```powershell
Invoke-WebRequest 'http://127.0.0.1:3000/' -UseBasicParsing
Invoke-WebRequest 'http://127.0.0.1:3000/assets/pen-skins/new-skin-id.png' -UseBasicParsing
```

确认返回成功，且页面引用的是最新版本号。改过服务端后要重启服务再测，不能只刷新浏览器。

### 4.3 浏览器运行时检查

至少用两个浏览器会话或设备验证：

1. 玩家 A 抽中/装备新皮肤。
2. 玩家 A 的预览、当前笔按钮和画手标签正确。
3. 玩家 B 看到 A 的远端笔，而不是默认笔。
4. A 画线时笔尖落点与实际线条起点对齐。
5. A 连续落笔、收笔，特效出现并自动清理。
6. B 看到同样的远端落笔/收笔表现，但没有收到图片上传或额外高频皮肤事件。
7. 进入下一轮、重连、刷新后仍使用正确皮肤。
8. 传说皮肤的视觉弹幕 A、B 都能看到，且不会遮挡操作。
9. 平板竖屏、横屏和输入法弹出时，抽卡/弹幕/画布仍可操作。
10. 开启系统减少动态效果后，页面仍有清楚的文字状态。

### 4.4 验收矩阵

| 检查项 | 通过标准 | 常见失败原因 |
| --- | --- | --- |
| 素材加载 | 本机预览和画板都显示新图 | 路径、大小写、缓存、PNG 损坏 |
| 本机装备 | 点击收藏后立即更新 | 未调用 `setPenCursorSkin` 或未保存收藏 |
| 远端装备 | 其他用户显示相同皮肤 | 忘记服务端白名单、未广播、远端仍固定经典笔 |
| 笔尖对齐 | 点击点与实际笔尖一致 | 缺少单独 anchor，或使用了视觉中心 |
| 特效 | start/end 出现并自动清理 | `effectId` 拼错、predicate 未接入、CSS 未定义 |
| 抽卡 | 稀有度、重复碎片、保底正常 | metadata 缺失、误放入免费池 |
| 传说弹幕 | 抽中者和旁观者都能看 | 服务端排除了发送者、客户端过滤自己消息 |
| 测试抽数 | 仅本机、当前游戏中有效 | 只做了前端判断，没有服务端校验 |
| 移动端 | 不挡输入栏和画布 | 固定定位未考虑安全区/输入法 |
| 回滚 | 删除新资源不会破坏旧收藏 | 复用了旧 ID、重置了 localStorage |

## 5. 常见问题与定位顺序

### 问题：别人看到的仍是默认笔

按这个顺序查：


1. `PEN_SKINS` 是否有该 ID。
2. `server.js` 的 `PEN_SKIN_IDS` 是否有该 ID。
3. 客户端发送的是否是 `skinId`，而不是显示名。
4. `set_pen_skin` 是否触发了 `broadcastPlayers`。
5. 远端 `state.players` 是否包含正确的 `penSkinId`。
6. `updateRemotePenCursor` 是否通过同一个 ID 找到了素材。
7. 浏览器是否命中旧的 `app.js` 缓存。

### 问题：笔迹和笔尖距离很远

先调 `PEN_CURSOR_ANCHORS`，不要修改画板坐标换算，也不要移动真实笔迹。检查素材是否被 `object-fit: contain` 缩放，以及原图四周是否有透明留白。

### 问题：有皮肤图但没有特效

检查 `effectId`、`isXPenSkin`、`spawnPenStartEffect`、`spawnPenEndEffect` 和对应 CSS。再确认当前不是系统减少动态效果模式，也不是特效层被旧 DOM 或层级规则遮住。

### 问题：抽中传说的人看不到自己的弹幕

检查服务端是否使用了带 `exceptId` 的广播，以及客户端是否写了“自己消息不显示”的过滤。传说视觉事件必须广播给发送者；声音可以按发送者单独去重。

### 问题：测试抽数的弹幕看不到

不要只检查普通弹幕开关。确认 `pen_gacha_grant` 已到达客户端，`#gachaGrantAnnouncement` 独立存在，且没有被普通 bullet 层的显示设置、输入层或 `z-index` 覆盖。

### 问题：服务端拒绝新皮肤

检查 ID 拼写、服务是否重启、房间阶段、WebSocket 对端地址和服务端白名单。前端显示“拥有”不代表服务端会接受未知 ID。

## 6. 回滚和下架原则

回滚应尽量是增量回滚：

1. 记录要下架的 `skinId`。
2. 停止抽卡池新增该 ID，但不要复用这个 ID。
3. 保留旧用户的 `owned` 数据，装备值发现未知时回退经典笔。
4. 移除或替换资源前先确认没有旧页面仍需要访问。
5. 逐项撤销 effect route、CSS、服务端白名单和前端注册。
6. 更新 manifest 的状态和回滚原因。
7. 清缓存后用拥有旧皮肤的用户模拟刷新、重连和加入房间。

不要用清空 `localStorage`、换掉整个收藏 key、覆盖旧图片或复用旧 ID 的方式“快速修复”。这些做法会损坏用户收藏并让问题难以追踪。

## 7. 最小交付模板

新增一支普通/史诗/传说笔时，提交内容应至少包括：

```text
[ ] 需求卡：主题、ID、稀有度、特效、来源和授权边界
[ ] public/assets/pen-skins/<asset>.png
[ ] public/assets/pen-skins/manifest.json
[ ] public/app.js：PEN_SKINS
[ ] public/app.js：PEN_CURSOR_ANCHORS
[ ] public/app.js：可选 effectId、predicate、start/end effect
[ ] public/style.css：可选特效样式、移动端、减少动态效果
[ ] server.js：PEN_SKIN_IDS
[ ] server.js：传说皮肤才加入 PEN_LEGENDARY_SKIN_IDS
[ ] public/index.html：缓存版本号（前端文件有修改时）
[ ] node --check public/app.js
[ ] node --check server.js
[ ] HTTP 页面和 PNG 检查
[ ] 本机装备检查
[ ] 远端显示检查
[ ] 笔尖四角校准检查
[ ] 抽卡、重复、保底检查
[ ] 传说弹幕本人/他人可见检查
[ ] 手机、平板、减少动态效果检查
[ ] 回滚路径确认
```

## 8. 明确禁止的做法

- 只添加图片，不注册 `PEN_SKINS`。
- 只修改前端，不加入服务端白名单。
- 让客户端把任意资源 URL、CSS 或脚本广播给其他玩家。
- 为了显示笔皮肤，新增每帧 WebSocket 事件。
- 把笔皮肤效果画进正式画布，导致无法撤销或影响猜图。
- 复用旧 `skinId` 表示新主题。
- 为调锚点修改全局画板坐标。
- 用清空收藏数据解决迁移问题。
- 只在 localhost 前端按钮里做权限判断。
- 使用未授权的第三方角色图、商标、字体或音效直接公开分发。
- 只看静态语法通过，就声称“已经验证远端同步”。

## 9. 一句话判断是否完成

> 新皮肤必须做到：素材可追溯、ID 可稳定识别、笔尖准确、自己和别人都能看到、特效只影响本地表现、抽卡和传说播报链路完整、移动端不挡操作、旧收藏可恢复，并且每一步都有实际验证或明确的未验证项。
